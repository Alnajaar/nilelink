# ------------------------------------------------------------------
# PRODUCTION API KEYS CHECKLIST
# Rename this file to .env.local for local dev, or add these to Cloudflare Pages
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# PRODUCTION API KEYS CHECKLIST
# ------------------------------------------------------------------

# 1. FIREBASE ADMIN SDK (Required for Affiliate Persistence & Secure API)
# Go to Firebase Console -> Project Settings -> Service Accounts -> "Generate new private key"
# Download the JSON file and copy the values here:
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CLIENT_EMAIL=your_service_account_email@...
# Copy the WHOLE private key including -----BEGIN PRIVATE KEY-----
# If setting in Cloudflare, use just the string content.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvgI..."


# 2. FIREBASE CONFIGURATION (Required for Auth in Production)
# Get these from Firebase Console -> Project Settings -> General -> Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your_real_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# 3. WALLET CONNECT (Optional - for robust Wallet support)
# Get project ID from https://cloud.reown.com/
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_id

# 4. APP CONFIGURATION
NEXT_PUBLIC_APP_URL=https://nilelink.app
NODE_ENV=production
