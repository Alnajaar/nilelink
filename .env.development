# NileLink Protocol - Development Configuration
# Local development environment configuration

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_CHAIN_ID=1337

# Database (PostgreSQL via Docker)
DATABASE_URL=postgresql://nilelink:nilelink123@localhost:5432/nilelink_dev

# Redis (via Docker)
REDIS_URL=redis://localhost:6379

# Backend API
NEXT_PUBLIC_API_URL=http://localhost:3001/api
API_BASE_URL=http://localhost:3001

# Hardhat Local Network
HARDHAT_RPC_URL=http://localhost:8545

# Blockchain Configuration (Development)
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
POLYGONSCAN_API_KEY=your-dev-polygonscan-key

# RPC Endpoints (Development)
MUMBAI_RPC_URL=http://localhost:8545
POLYGON_RPC_URL=http://localhost:8545

# Contract Addresses (will be deployed to local network)
USDC_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
FEE_RECIPIENT=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Security (Development keys - NEVER use in production)
JWT_SECRET=nilelink-dev-jwt-secret-key-change-in-production
ENCRYPTION_KEY=nilelink-dev-encryption-key-32-chars-exactly!!!

# External Services (Development)
ALCHEMY_API_KEY=dev-key
INFURA_PROJECT_ID=dev-project
MORALIS_API_KEY=dev-key

# Email & Notifications (Development - using console)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=dev
SMTP_PASS=dev
FROM_EMAIL=dev@nilelink.local

# File Storage (Local development)
AWS_ACCESS_KEY_ID=dev-key
AWS_SECRET_ACCESS_KEY=dev-secret
AWS_S3_BUCKET=nilelink-dev

# Monitoring (Development)
SENTRY_DSN=
LOG_LEVEL=debug

# Feature Flags (Development)
ENABLE_ADVANCED_ANALYTICS=true
ENABLE_AI_RECOMMENDATIONS=true
ENABLE_BLOCKCHAIN_INTEGRATION=true
ENABLE_REAL_TIME_UPDATES=true

# Web App URLs (for cross-app communication)
NEXT_PUBLIC_CUSTOMER_URL=http://localhost:3002
NEXT_PUBLIC_POS_URL=http://localhost:3003
NEXT_PUBLIC_DELIVERY_URL=http://localhost:3004
NEXT_PUBLIC_SUPPLIER_URL=http://localhost:3005
NEXT_PUBLIC_PORTAL_URL=http://localhost:3006
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3007
NEXT_PUBLIC_UNIFIED_URL=http://localhost:3008