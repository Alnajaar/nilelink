{
    "name": "nilelink-ipfs-token",
    "version": "1.0.0",
    "description": "Cloudflare Worker for issuing IPFS upload tokens",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "deploy:production": "wrangler deploy --env production"
    },
    "dependencies": {
        "@tsndr/cloudflare-worker-jwt": "^2.4.2",
        "ethers": "^6.9.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20231218.0",
        "typescript": "^5.3.3",
        "wrangler": "^3.22.0"
    }
}