{
    "name": "nilelink-ipfs-upload",
    "version": "1.0.0",
    "description": "Cloudflare Worker for proxying IPFS uploads to Pinata",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "deploy:production": "wrangler deploy --env production"
    },
    "dependencies": {
        "@tsndr/cloudflare-worker-jwt": "^2.5.3"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250124.0",
        "typescript": "^5.7.3",
        "wrangler": "^3.101.0"
    }
}