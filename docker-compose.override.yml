# Development overrides for docker-compose.yml
version: '3.8'

services:
  # Development overrides for backend
  backend:
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true # For file watching in containers
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/logs:/app/logs
    command: npm run dev

  # Override web services for development
  web-customer:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/customer:/app
      - /app/node_modules
      - /app/.next

  web-pos:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/pos:/app
      - /app/node_modules
      - /app/.next

  web-delivery:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/delivery:/app
      - /app/node_modules
      - /app/.next

  web-supplier:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/supplier:/app
      - /app/node_modules
      - /app/.next

  web-portal:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/portal:/app
      - /app/node_modules
      - /app/.next

  web-dashboard:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/dashboard:/app
      - /app/node_modules
      - /app/.next

  web-unified:
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
      - API_URL_INTERNAL=http://backend:3001/api
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./web/unified:/app
      - /app/node_modules
      - /app/.next
